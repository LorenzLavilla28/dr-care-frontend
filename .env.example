# Optional for development. The app falls back to a normal address field when blank.
# Restrict this browser key to localhost and the production web origin in Google Cloud.
VITE_GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_MAP_ID=DEMO_MAP_ID

# Optional API override. Defaults to http://localhost:8080.
VITE_API_URL=http://localhost:8080
